import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { Integrals0Page } from './integrals0';

@NgModule({
  declarations: [
    Integrals0Page,
  ],
  imports: [
    IonicPageModule.forChild(Integrals0Page),
  ],
})
export class Integrals0PageModule {}
